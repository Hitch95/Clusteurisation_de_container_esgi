apiVersion: v1
kind: ResourceQuota
metadata:
  name: staging-quota
  namespace: app-staging
spec:
  hard:
    pods: "15"
    requests.cpu: "4"
    requests.memory: "4Gi"
    limits.cpu: "8"
    limits.memory: "8Gi"
    services: "10"
    persistentvolumeclaims: "10"
