version: "3.8"

services:
  api:
    image: ${IMAGE_REF}
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure